spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/airesume_db
    username: postgres
    password: Bhawani$969460
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

    open-in-view: false

server:
  port: 8080


app:
  file:
    upload-dir: uploads/resumes